// Named export
export const greetUser = (name) => {
    return `Hello ${name}! Welcome to Day 12.`;
};

export const greetMorning = (name) => {
    return `Good morning ${name}!`;
};

// Default export
const appName = "MERN Learning App";
export default appName;